kind: Service
apiVersion: v1
metadata:
  name: kafka-server
  labels:
    srv: kafka-server
spec:
  type: ClusterIP
  selector:
    srv: kafka-server
  ports:
  - name: kafka
    protocol: TCP
    port: 9092
    targetPort: 9092
