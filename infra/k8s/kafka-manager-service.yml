kind: Service
apiVersion: v1
metadata:
  name: kafka-manager
  labels:
    srv: kafka-manager
spec:
  type: ClusterIP
  selector:
    srv: kafka-manager
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 9000