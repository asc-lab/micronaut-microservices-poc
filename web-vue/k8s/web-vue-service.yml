kind: Service
apiVersion: v1
metadata:
  name: web-vue
  labels:
    srv: web-vue
spec:
  type: ClusterIP
  selector:
    srv: web-vue
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
