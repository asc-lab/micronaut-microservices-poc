kind: Service
apiVersion: v1
metadata:
  name: product-service
  labels:
    srv: product-service
spec:
  type: ClusterIP
  selector:
    srv: product-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
