kind: Service
apiVersion: v1
metadata:
  name: policy-service
  labels:
    srv: policy-service
spec:
  type: ClusterIP
  selector:
    srv: policy-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80