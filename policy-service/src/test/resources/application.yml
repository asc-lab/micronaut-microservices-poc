micronaut:
  application:
    name: policy-service
  server:
    host: localhost
    port: ${SERVICE_PORT:-1}
---
datasources:
  default:

---
jpa:
 default:
    properties:
        hibernate:
            hbm2ddl:
                auto: create-drop
            show_sql: true
---
jackson:
  serialization:
    failOnEmptyBeans: false
    writeDatesAsTimestamps: true
---
kafka:
